# ───────── Notebook 01 — Dataset ─────────
datasets==2.19.0
pandas==2.2.2
matplotlib==3.9.0

# ───────── Notebook 02 — Fine-tuning (Colab GPU) ─────────
# Instalado dentro do próprio notebook; mantenha aqui para referência.
# transformers==4.51.3
# peft==0.15.2
# trl==0.16.1
# bitsandbytes==0.45.5
# accelerate==1.6.0

# ───────── Assistente (LangChain + RAG + LangGraph + Streamlit) ─────────
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-text-splitters>=0.3.0,<0.4
langchain-ollama>=0.2.0,<0.4
langchain-huggingface>=0.1.0,<0.4
langchain-chroma>=0.1.4,<0.3
langgraph>=0.2.40,<0.4
chromadb>=0.5.0,<0.7
sentence-transformers>=3.0.0
streamlit>=1.36.0
